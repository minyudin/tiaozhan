# 图1-2 提示词（改图法：在原论文图基础上修改）

**图注**：图1-2：(a)EcoCitySociety 平台总体功能框架 (b)政策输入—智能体配置—训练搜索—评估输出四大功能环节流程图

**做法**：一张一张改，改完后再拼接为 (a)(b) 组图。

---

## 面板 (a) —— 以 `ugi_fig1.png`（UGI, arXiv:2312.11813 Figure 1）为底图修改

Edit this figure while strictly keeping its original visual style: same layered layout, same rounded rectangles, same dashed group borders, same colors, same arrow style, same font feel. Only change the content as follows:

1. Change the structure from 3 groups to 5 stacked layers (bottom to top). Rotate the left-side vertical group labels to Chinese:
   - Bottom layer label "数据底座层", containing three light-blue boxes: "多源城市数据", "数字孪生底座", "流式数据接入".
   - Second layer label "仿真引擎层", containing three boxes: "22 种环境仿真器", "多智能体系统", "社会网络演化".
   - Third layer label "智能训练层", containing three boxes: "MARL 训练工厂", "超参数搜索", "榜单管理".
   - Fourth layer label "分析评估层", containing three boxes: "因果推断", "反事实推理", "Benchmark 评估".
   - Top layer label "应用服务层", containing four gray boxes: "WebAPI", "CLI", "Dashboard", "政策推演应用".
2. Remove the "CityGPT" side box and the green "Natural Language Interface" ellipse; instead keep simple upward black arrows between adjacent layers, exactly like the original arrow style.
3. All text in Simplified Chinese except technical acronyms (WebAPI, CLI, Dashboard, MARL, Benchmark). Render every Chinese character accurately.
4. Do not add any new decorative elements, icons, shadows, or gradients. Keep the plain white background.

## 面板 (b) —— 以 `oasis_fig2.png`（OASIS, arXiv:2411.11581 Figure 2）为底图修改

Edit this figure while strictly keeping its original visual style: same rounded module containers with thin colored borders and colored italic module titles, same small flat icons, same labeled arrows, same fonts and palette. Only change the content as follows:

1. Keep exactly four main module containers arranged left to right, connected by the original purple workflow arrows:
   - Module 1 (blue border), title "政策输入": inside, a document icon and text "政策情景 Spec 描述".
   - Module 2 (green border), title "智能体配置": inside, small people icons and two lines "36 种社会角色", "人口批量生成".
   - Module 3 (orange border), title "训练搜索": inside, a gear/brain icon and two lines "MARL 训练", "超参搜索".
   - Module 4 (purple border), title "评估输出": inside, a small bar-chart icon and two lines "因果评估", "报告溯源导出".
2. Remove all other original modules, the top legend, and the OASIS logo.
3. Add one gray dashed arrow going from Module 4 back under the row to Module 1, labeled "迭代优化", in the same dashed style used in the original figure.
4. All text in Simplified Chinese except acronyms (Spec, MARL). Render every Chinese character accurately.
5. Keep the plain white background; no shadows, no gradients, no extra decorations.

## 拼接

两张图分别生成满意后，左右并排拼为一张组图，左上角分别加粗标注 (a)、(b)，中间留白分隔。
