# 图2-3 提示词与素材

**图注**：图2-3：(a)统一 Simulator/Process 抽象类图 (b)自适应步长控制稳定性对比曲线 (c)并行 Runner 加速比测试图

**素材说明**：
- 面板 (a)：改图法生成，底图 `agentscope_fig1.png`（AgentScope, Gao et al., arXiv:2402.14034, Figure 1，虚线分层模块架构图）
- 面板 (b)(c)：数据图，已用 matplotlib 绘制完成（`fig2-3bc.png`），无需生成；当前为演示数据，之后可用平台实测数据重绘

---

## 面板 (a) —— 以 `agentscope_fig1.png` 为底图修改

在这张图的基础上修改，严格保持原图的视觉风格：保留原有的深蓝色虚线分层容器、白底细线内部方块、左侧竖排层名标签的排版，只按以下要求修改内容：

1. 最上层左侧竖排标签改为「仿真器层」，容器内部：顶部长条方块「Simulator 抽象基类：step() 统一接口 · SimulatorState 状态管理」；下方并排小方块「交通仿真器」「空气质量仿真器」「经济仿真器」「人口仿真器」「…（22 种）」；
2. 中间层左侧竖排标签改为「过程组件层」，容器内部：左侧方块组「Process 环境过程抽象」（内含「扩散过程」「反应过程」「输运过程」），中间竖条「ProcessLibrary 可组合过程库」，右侧方块组「自适应步长控制器」（内含「变化率监测」「振荡监测」「NaN 检测」）；
3. 最下层左侧竖排标签改为「运行层」，容器内部：左侧方块组「并行 Runner」（内含「多进程」「Ray」「Dask」），右侧方块组「可信研究闭环」（内含「Spec 规范描述」「Provenance 溯源」「Benchmark 基准」「Methods 导出」）；
4. 右侧独立竖排容器标题改为「交互接口」，内部竖排方块「WebAPI」「CLI」「Dashboard」「监控剖析」；
5. 删除原图中所有品牌 logo。除英文类名（Simulator、Process、Spec 等）外全部使用简体中文，每个汉字必须渲染准确；纯白背景。

## 拼接

生成好 (a) 后发回，与已画好的 (b)(c)（`fig2-3bc.png`）拼接：(a) 在左，(b)(c) 在右侧或下方，左上角加粗标注 (a)，留白分隔。
