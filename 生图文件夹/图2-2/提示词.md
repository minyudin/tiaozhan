# 图2-2 提示词（改图法：在参考论文图基础上修改）

**图注**：图2-2：平台五层总体架构图（单图，非组图）

**参考来源**（新论文）：
- `aios_fig2.png`（首选底图）— AIOS: LLM Agent Operating System, Mei et al., arXiv:2403.16971, Figure 2（虚线分隔的分层架构：每层右侧标注层名，层内为彩色圆角模块，风格干净、学术感强）
- `ioa_fig1.png`（备选底图）— Internet of Agents, Chen et al., arXiv:2407.07061, Figure 1（左侧竖排层名+高密度模块卡片画法）
- 旧参考（保留备查）：`ufm_fig3.png`、`agentsociety_fig1.png`

---

## 首选：以 `aios_fig2.png` 为底图修改

在这张图的基础上修改，严格保持原图的视觉风格：保留虚线分隔的水平分层布局、每层右侧的斜体层名标注、层内彩色圆角小模块和黑色细箭头连接，只按以下要求修改内容：

1. 改为五层，自下而上依次为：
   - 第1层（最底层）层名「数据采集层（感知层）」：内部模块「IoT 设备」「移动终端」「外部 API」「气候/交通/社会经济数据」；
   - 第2层层名「数据处理与分析层（处理层）」：内部模块「数据清洗与异常检测」「机器学习/深度学习建模」「预测分析」「风险评估」；
   - 第3层层名「智能决策与仿真层（决策层）」：中央大容器标题「EcoCitySociety 仿真决策核心」，内部一排彩色小模块「多智能体系统」「仿真模拟」「优化调度」「决策支持」；
   - 第4层层名「应用服务与执行层（执行层）」：内部模块「用户界面 UI/UX」「数据服务 API」「业务流程支持」；
   - 第5层（最顶层）层名「监控与反馈层（监控层）」：内部模块「系统状态监控」「数据质量监控」「异常预警」「自我优化」；
2. 层与层之间保留虚线分隔和上下箭头连接，最顶层到最底层可加一条右侧回环箭头标注「反馈优化」；
3. 删除原图中所有原有英文模块名（Travel Agent、AIOS Kernel、CPU 等）。除 IoT、API、UI/UX 等通用缩写外全部使用简体中文，每个汉字必须渲染准确；纯白背景。

## 备选：以 `ioa_fig1.png` 为底图修改

若想要更高信息密度的卡片式画法，用此底图：保留左侧竖排层名和圆角卡片模块风格，把三层改为上述五层（层名竖排），每层内放对应模块卡片，内容同上。
