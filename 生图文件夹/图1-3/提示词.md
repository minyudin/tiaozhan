# 图1-3 提示词（改图法：在原论文图基础上修改）

**图注**：图1-3：(a)可复现/模块化/可扩展/可组合核心理念四象限图 (b)可信研究闭环（Spec/Provenance/Benchmark/Methods）示意 (c)工程化落地能力技术栈图

**做法**：一张一张改，改完后再拼接为 (a)(b)(c) 组图。

---

## 面板 (a) —— 以 `agentsociety_fig1.png`（AgentSociety, arXiv:2502.08691 Figure 1）为底图修改

Edit this figure while strictly keeping its original visual style: same rounded rectangle containers, same soft colors, same small flat icons, same fonts. Only change the content as follows:

1. Replace all content with a 2x2 quadrant layout of four equally sized rounded boxes, each with a small flat icon and a Chinese keyword plus one short line:
   - Top-left (blue): "可复现" with subtitle "结果可复现导出".
   - Top-right (green): "模块化" with subtitle "子系统独立可替换".
   - Bottom-left (orange): "可扩展" with subtitle "百万级智能体扩展".
   - Bottom-right (purple): "可组合" with subtitle "模块即插即用组合".
2. Remove all original text and modules (Scale, Toolbox, Behaviors, Minds, robot illustration, etc.).
3. All text in Simplified Chinese, rendered accurately. Keep plain white background; no shadows, gradients, or extra decorations.

## 面板 (b) —— 以 `oasis_fig2.png`（OASIS, arXiv:2411.11581 Figure 2）为底图修改

Edit this figure while strictly keeping its original visual style: same rounded module containers with thin colored borders and colored italic module titles, same labeled arrows, same palette. Only change the content as follows:

1. Keep four modules arranged in a clockwise closed loop, connected by curved arrows in the original arrow style:
   - Module 1 (blue), title "Spec 规范化描述": inside, a document icon and text "模型与场景可描述".
   - Module 2 (green), title "Provenance 溯源": inside, a magnifier icon and text "参数与数据可追".
   - Module 3 (orange), title "Benchmark 评估": inside, a bar-chart icon and text "指标与基准可比".
   - Module 4 (purple), title "Methods 导出": inside, an export icon and text "结果可复现".
2. Center label "可信研究闭环" in gray.
3. Remove all other original modules, the legend, and the OASIS logo. All text in Simplified Chinese except acronyms. Plain white background.

## 面板 (c) —— 以 `agentsociety_fig10.png`（AgentSociety, arXiv:2502.08691 Figure 10）为底图修改

Edit this figure while strictly keeping its original visual style: same dashed group borders, same purple/blue bars, same stacked layer look. Only change the content as follows:

1. Top dashed group titled "交互层", containing three boxes: "WebAPI", "CLI", "Dashboard".
2. Middle dashed group titled "运行层", containing boxes: "多进程 Runner", "Ray 并行", "Dask 并行", "监控剖析".
3. Bottom dashed group titled "部署与质量层", containing boxes: "Docker 容器化", "K8s 编排", "回归测试", "属性测试".
4. Connect the three groups with the original vertical arrow style. Remove all original logos (deepseek, EMQX, PostgreSQL, mlflow) and all original text.
5. All text in Simplified Chinese except technical acronyms (WebAPI, CLI, Dashboard, Ray, Dask, Docker, K8s). Render every Chinese character accurately. Plain white background.

## 拼接

三张图分别生成满意后，拼为组图（(a)(b) 上排、(c) 下排或横向三联），左上角分别加粗标注 (a)、(b)、(c)，留白分隔。
